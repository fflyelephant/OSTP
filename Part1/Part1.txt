Virtual Memory goals:
1:transparency(透明度)	
	实现虚拟内存对应用程序来说是透明不可见的.
2:efficiency(高效)
	2.1 不会使应用程序运行速率变慢(时间)
	2.1 不会使用过多的内存用以支持内存虚拟化操作(空间)
3:protection/isolation(保护性,也可以说独立性)
	程序之间的虚拟内存操作应有独立性, 不可互访和修改其它程序的虚拟内存中内容

